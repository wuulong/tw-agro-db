# 🤖 tw-agro-db Multi-Agent 跨域意圖路由手冊 (WORKFLOW.md)

本手冊定義 LLM 農務 Agent / Multi-Agent 系統在調用 `tw-agro-db` 時的標準意圖路由與長鏈推理工作流。

---

## 🔀 1. Multi-Agent 意圖路由地圖

```
                    [使用者查詢 Prompt]
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 👑 A00 全域檢索 (fts_agro_global)    │
        │ Command: python main.py search "<KW>" │
        └───────────────────┬───────────────────┘
                            │
         ┌──────────────────┼──────────────────┐
         ▼                  ▼                  ▼
┌─────────────────┐┌─────────────────┐┌───────────────────┐
│ 🌾 Pillar 1     ││ 🐟 Pillar 2     ││ 🐖 Pillar 3       │
│ A10 農作物行情   ││ A20 水產行情    ││ A30 毛豬與肉品行情│
│ A11 農藥安全停藥 ││ A21 養殖水質    ││ A31 動物用藥殘留  │
└────────┬────────┘└────────┬────────┘└─────────┬─────────┘
         │                  │                   │
         └──────────────────┼───────────────────┘
                            ▼
        ┌───────────────────────────────────────┐
        │ 📊 A00 母大腦延伸分析與風險註冊表    │
        │ Command: python main.py analytics     │
        └───────────────────┬───────────────────┘
                            ▼
               [最終安全綜合解答 / 簡報導出]
```

---

## 🧭 2. 典型跨域推理工作流 (Agent Long-Chain Workflow)

### 場景 1: 「甘藍（高麗菜）價格過高，現在適合採收銷售嗎？用藥安全嗎？」
1. **第一步 (全域檢索)**：呼叫 `python main.py search "甘藍"`，確認其 `crop_id = LA`。
2. **第二步 (A10 行情穿透)**：呼叫 `python main.py a10 search "甘藍"` 取得近期台北一、台北二等市場之平均價格與價差波幅 `spread_pct`。
3. **第三步 (A00 延伸分析)**：呼叫 `python main.py analytics cross-market --crop-id LA` 檢視全台價格離散係數 $CV$。若 $CV > 0.35$，提示 Agent「各地區價差大，宜選擇北部市場拋售」。
4. **第四步 (A11 農藥對比)**：連動 `A11` 查詢噴灑農藥之安全採收停藥期（如 7 天），出具確定性合規採收報告。
